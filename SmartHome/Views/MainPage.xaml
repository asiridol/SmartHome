<?xml version="1.0" encoding="utf-8"?>
<local:ViewModelPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:local="clr-namespace:SmartHome.Views;assembly:SmartHome" x:Class="SmartHome.Views.MainPage" x:TypeArguments="vm:MainPageViewModel" xmlns:vm="clr-namespace:SmartHome.ViewModels;assembly=SmartHome">
    <StackLayout>
        <!-- Place new controls here -->
    <ScrollView HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
        <StackLayout VerticalOptions="CenterAndExpand" Padding="20">
            <Image HorizontalOptions="Center" Margin="20" Aspect="AspectFill" WidthRequest="200" HeightRequest="100" BackgroundColor="Gray" />
            <Label Text="Email address" VerticalOptions="Start" HorizontalOptions="FillAndExpand" />
            <Entry Placeholder="Email address" VerticalOptions="Start" HorizontalOptions="FillAndExpand" Keyboard="Email" Text="{Binding UserName}" />
            <Label Text="Password" VerticalOptions="Start" HorizontalOptions="FillAndExpand" />
            <Entry Placeholder="Password" VerticalOptions="Start" HorizontalOptions="FillAndExpand" IsPassword="true" Text="{Binding Password}" />
            <Button Text="Log in" VerticalOptions="Start" HorizontalOptions="FillAndExpand" Margin="0,30,0,30" Command="{Binding LoginCommand}" />
        </StackLayout>
    </ScrollView>
    </StackLayout>
</local:ViewModelPage>